{% comment %}
  Store Homepage Section
  Features hero section with CTAs and features grid
{% endcomment %}

<div class="store-homepage">
  <!-- Hero Section -->
  <div class="hero-section">
    <div class="hero-image-wrapper">
      <video 
        class="hero-video"
        autoplay 
        muted 
        loop 
        playsinline
        preload="auto"
      >
        <source src="https://hbfjdfxephlczkfgpceg.supabase.co/storage/v1/object/public/website/demos/Untitled%20video%20-%20Made%20with%20Clipchamp%20(18)%20(1).mp4" type="video/mp4">
        Your browser does not support the video tag.
      </video>
      <div class="hero-overlay"></div>
    </div>
    
    <div class="hero-content">
      <p class="hero-tagline">Be Different</p>
    </div>
  </div>

  <!-- Horizontal Product Carousel -->
  <div class="product-carousel-section">
    <div class="page-width">
      <div class="carousel-header">
        <h2 class="carousel-title">Featured Floor Plans</h2>
      </div>
      
      <div class="carousel-container">
        <div class="carousel-track" id="carousel-track">
        <!-- Products will be loaded here dynamically -->
        <div class="carousel-loading">
          <svg width="40" height="40" viewBox="0 0 40 40">
            <circle cx="20" cy="20" r="18" fill="none" stroke="#3A3A3A" stroke-width="4"/>
            <circle cx="20" cy="20" r="18" fill="none" stroke="#B8860B" stroke-width="4" stroke-dasharray="90, 150" stroke-linecap="round">
              <animateTransform attributeName="transform" type="rotate" from="0 20 20" to="360 20 20" dur="1s" repeatCount="indefinite"/>
            </circle>
          </svg>
        </div>
      </div>
      
      <div class="carousel-footer">
        <a href="/pages/plans" class="view-all-link">
          View All Plans
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"></line>
            <polyline points="12 5 19 12 12 19"></polyline>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- How It Works Section -->
  <section class="how-it-works-section">
    <div class="page-width">
      <h2 class="section-title">How It Works</h2>
      <p class="section-subtitle">Your dream home is just three simple steps away</p>
      
      <div class="steps-grid">
        <!-- Step 1 -->
        <div class="step-card">
          <div class="step-icon">
            {% render 'custom-icon', icon: 'zoom', size: 56, alt: 'Browse Plans Icon' %}
          </div>
          <div class="step-content">
            <div class="step-number">01</div>
            <h3 class="step-title">Browse Plans</h3>
            <p class="step-description">Explore our collection of professionally designed floor plans. Filter by size, style, and features to find your perfect match.</p>
            <ul class="step-features">
              <li>✓ 400+ unique designs</li>
              <li>✓ Detailed specifications</li>
              <li>✓ 3D visualizations</li>
            </ul>
          </div>
        </div>
        
        <!-- Step 2 -->
        <div class="step-card">
          <div class="step-icon">
            {% render 'custom-icon', icon: 'card', size: 56, alt: 'Purchase & Download Icon' %}
          </div>
          <div class="step-content">
            <div class="step-number">02</div>
            <h3 class="step-title">Purchase & Download</h3>
            <p class="step-description">Complete your secure purchase and instantly download your comprehensive plan package with all the files you need.</p>
            <ul class="step-features">
              <li>✓ Instant digital delivery</li>
              <li>✓ Multiple file formats</li>
              <li>✓ Construction-ready plans</li>
            </ul>
          </div>
        </div>
        
        <!-- Step 3 -->
        <div class="step-card">
          <div class="step-icon">
            {% render 'custom-icon', icon: 'tool', size: 56, alt: 'Build Your Dream Icon' %}
          </div>
          <div class="step-content">
            <div class="step-number">03</div>
            <h3 class="step-title">Build Your Dream</h3>
            <p class="step-description">Take your plans to your builder or contractor and start construction. Our detailed plans make the building process smooth and efficient.</p>
            <ul class="step-features">
              <li>✓ Builder-approved format</li>
              <li>✓ Permit-ready documents</li>
              <li>✓ Ongoing support included</li>
            </ul>
          </div>
        </div>
      </div>
      
      <div class="how-it-works-cta">
        <a href="/pages/plans" class="cta-button-primary">
          Start Browsing Plans
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"></line>
            <polyline points="12 5 19 12 12 19"></polyline>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Social Proof Section -->
  <section class="social-proof-section">
    <div class="page-width">
      <!-- Featured Project Video -->
      <div class="featured-project-container">
        <div class="featured-project-video">
          <div class="video-wrapper">
            <video 
              id="featured-project-video"
              class="featured-video"
              loop
              muted
              playsinline
              preload="metadata"
            >
              <source src="https://hbfjdfxephlczkfgpceg.supabase.co/storage/v1/object/public/website/demos/Untitled%20video%20-%20Made%20with%20Clipchamp%20(19)%20(1).mp4" type="video/mp4">
              Your browser does not support the video tag.
            </video>
          </div>
          <div class="featured-project-info">
            <h3>Spring Mountain Estate</h3>
            <p class="project-location">📍 Hill Country, Texas</p>
            <p class="project-description">A stunning 4,500 sq ft custom home featuring modern industrial design with panoramic hill country views.</p>
          </div>
        </div>
      </div>

      <script>
        // Auto-play video when it comes into view
        document.addEventListener('DOMContentLoaded', function() {
          const video = document.getElementById('featured-project-video');
          
          // Use Intersection Observer to start playing when visible
          const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
              if (entry.isIntersecting) {
                // Video is in view - play it
                video.play().catch(error => {
                  console.log('Autoplay prevented:', error);
                });
              } else {
                // Video is out of view - pause it to save resources
                video.pause();
              }
            });
          }, { threshold: 0.25 }); // Play when 25% visible
          
          observer.observe(video);
        });
      </script>
    </div>
  </section>

  <!-- Brand Story Section -->
  <section class="brand-story-section">
    <div class="page-width">
      <div class="brand-story-grid">
        <!-- Image Side -->
        <div class="brand-story-image">
          <img src="https://hbfjdfxephlczkfgpceg.supabase.co/storage/v1/object/public/website/floorplans/Copy%20of%20Apex.webp" 
               alt="Barnhaus Floor Plan Design">
        </div>
        
        <!-- Content Side -->
        <div class="brand-story-content">
          <div class="content-badge">400+ Plans Designed</div>
          <h2 class="brand-story-title">Designed by Custom Home Experts</h2>
          
          <div class="brand-story-text">
            <p>
              Every plan in our collection is designed by the team at <strong>Barnhaus Steel Builders</strong>—a 
              premium design service backed by real-world building experience. We understand what makes a home 
              both beautiful and buildable because we've been there on the job site.
            </p>
            <p>
              Our plans aren't just drawings—they're the result of years of hands-on construction knowledge. 
              Each design reflects practical building experience, ensuring your builder can execute with confidence.
            </p>
          </div>
          
          <!-- Features -->
          <div class="brand-features">
            <div class="feature-item">
              <span class="feature-icon">✓</span>
              <span>47+ Custom Homes Built</span>
            </div>
            <div class="feature-item">
              <span class="feature-icon">✓</span>
              <span>Steel Frame Specialists</span>
            </div>
            <div class="feature-item">
              <span class="feature-icon">✓</span>
              <span>Transparent Pricing</span>
            </div>
          </div>
          
          <!-- CTAs -->
          <div class="brand-story-ctas">
            <a href="/pages/plans" class="btn btn-primary">
              Browse Catalog
            </a>
            <a href="/pages/contact" class="btn btn-secondary">
              Contact Us
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Custom Design Studio CTA Section -->
  <section class="custom-studio-section">
    <div class="page-width">
      <div class="studio-cta-container">
        <div class="studio-cta-content">
          <div class="studio-badge">✨ Custom Design</div>
          <h2 class="studio-title">Can't Find What You're Looking For?</h2>
          <p class="studio-subtitle">Try our Concept Studio to create your own custom floor plan and get a price for a completely unique design tailored to your vision.</p>
          
          <div class="studio-features">
            <div class="studio-feature">
              {% render 'custom-icon', icon: 'notebook', size: 32, alt: 'Design Your Vision Icon' %}
              <div>
                <h4>Design Your Vision</h4>
                <p>Start from scratch or modify existing plans</p>
              </div>
            </div>
            
            <div class="studio-feature">
              {% render 'custom-icon', icon: 'calculator', size: 32, alt: 'Instant Pricing Icon' %}
              <div>
                <h4>Instant Pricing</h4>
                <p>Get a custom quote based on your specifications</p>
              </div>
            </div>
            
            <div class="studio-feature">
              {% render 'custom-icon', icon: 'chatBubble', size: 32, alt: 'Expert Guidance Icon' %}
              <div>
                <h4>Expert Guidance</h4>
                <p>Work with our design team every step of the way</p>
              </div>
            </div>
          </div>
          
          <div class="studio-cta-buttons">
            <a href="https://3d.barnhaussteelbuilders.com" class="btn-studio-primary" target="_blank" rel="noopener">
              Launch Concept Studio
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </a>
            <a href="/pages/contact" class="btn-studio-secondary">Contact Our Team</a>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>

<style>
/* Store Homepage Styles */
.store-homepage {
  background: #1C1C1C;
}

/* Page Width Container - Aligns with header (1200px) */
.page-width {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1.5rem;
}

/* Hero Section */
.hero-section {
  position: relative;
  height: 60vh;
  min-height: 400px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  margin-top: 0;
  padding-top: 0;
}

.hero-image-wrapper {
  position: absolute;
  inset: 0;
  background: #1C1C1C;
}

.hero-video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  background: #1C1C1C;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, rgba(28, 28, 28, 0.3), rgba(28, 28, 28, 0.4));
}

.hero-content {
  position: relative;
  z-index: 1;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: flex-end;
  justify-content: flex-end;
  padding: 2rem;
}

.hero-tagline {
  font-size: 2.5rem;
  font-weight: 700;
  color: #1C1C1C;
  margin: 0;
  letter-spacing: 1px;
  text-shadow: 
    0 0 20px rgba(255, 255, 255, 0.9),
    0 0 40px rgba(255, 255, 255, 0.7),
    0 0 60px rgba(255, 255, 255, 0.5),
    0 0 80px rgba(255, 255, 255, 0.3),
    0 0 100px rgba(255, 255, 255, 0.2);
  filter: drop-shadow(0 0 30px rgba(255, 255, 255, 0.8))
          drop-shadow(0 0 50px rgba(255, 255, 255, 0.5));
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .hero-section {
    height: 75vh;
    min-height: 400px;
    margin-top: 0;
    padding-top: 0;
  }
  
  .hero-content {
    padding: 1.5rem;
  }
  
  .hero-tagline {
    font-size: 1.75rem;
    text-shadow: 
      0 0 15px rgba(255, 255, 255, 0.9),
      0 0 30px rgba(255, 255, 255, 0.7),
      0 0 45px rgba(255, 255, 255, 0.5),
      0 0 60px rgba(255, 255, 255, 0.3);
    filter: drop-shadow(0 0 20px rgba(255, 255, 255, 0.8))
            drop-shadow(0 0 40px rgba(255, 255, 255, 0.5));
  }
  
  /* Ensure video starts at very top */
  .hero-image-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
  }
}

/* Horizontal Product Carousel */
.product-carousel-section {
  padding: 4rem 0;
  background: #1C1C1C;
  overflow: hidden;
}

.carousel-header {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 2rem;
}

.carousel-title {
  font-size: 2.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.carousel-footer {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: 2rem;
}

.view-all-link {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  color: #1C1C1C;
  font-weight: 700;
  font-size: 0.9375rem;
  text-decoration: none;
  transition: all 0.3s ease;
  border-radius: 8px;
  border: 2px solid transparent;
  box-shadow: 0 4px 12px rgba(184, 134, 11, 0.3);
}

.view-all-link:hover {
  background: linear-gradient(135deg, #FFD700 0%, #B8860B 100%);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(184, 134, 11, 0.5);
  gap: 0.875rem;
}

.view-all-link svg {
  transition: transform 0.3s ease;
  stroke-width: 2.5;
}

.view-all-link:hover svg {
  transform: translateX(4px);
}

.carousel-container {
  position: relative;
  margin: 0 -1.5rem;
}

.carousel-track {
  display: flex;
  gap: 1.5rem;
  overflow-x: auto;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
  scrollbar-color: #B8860B #2A2A2A;
  padding: 0 1.5rem 1rem 1.5rem;
}

.carousel-track::-webkit-scrollbar {
  height: 8px;
}

.carousel-track::-webkit-scrollbar-track {
  background: #2A2A2A;
  border-radius: 4px;
}

.carousel-track::-webkit-scrollbar-thumb {
  background: #B8860B;
  border-radius: 4px;
}

.carousel-track::-webkit-scrollbar-thumb:hover {
  background: #FFD700;
}

.carousel-card {
  flex: 0 0 320px;
  background: #2A2A2A;
  border: 1px solid #3A3A3A;
  border-radius: 12px;
  overflow: hidden;
  transition: all 0.3s ease;
  text-decoration: none;
  display: flex;
  flex-direction: column;
}

.carousel-card:hover {
  border-color: #B8860B;
  box-shadow: 0 8px 25px rgba(184, 134, 11, 0.3);
  transform: translateY(-5px);
}

.carousel-card-image {
  width: 100%;
  height: 320px;
  object-fit: contain;
  background: #1C1C1C;
}

.carousel-card-content {
  padding: 1.25rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.carousel-card-title {
  font-size: 1.1rem;
  font-weight: 700;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.3;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.carousel-card-specs {
  display: none;
}

.carousel-card-price {
  color: #B8860B;
  font-size: 1.5rem;
  font-weight: 700;
}

.carousel-loading {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 400px;
  width: 100%;
}

/* Carousel Mobile Responsive */
@media (max-width: 768px) {
  .product-carousel-section {
    padding: 3rem 0;
  }
  
  .carousel-header {
    justify-content: center;
    margin-bottom: 1.5rem;
  }
  
  .carousel-title {
    font-size: 1.75rem;
    text-align: center;
  }
  
  .carousel-footer {
    margin-top: 1.5rem;
  }
  
  .carousel-container {
    margin: 0 -1rem;
  }
  
  .carousel-track {
    padding: 0 1rem 1rem 1rem;
  }
  
  .carousel-card {
    flex: 0 0 280px;
  }
  
  .carousel-card-image {
    height: 280px;
  }
  
  .carousel-card-content {
    padding: 1rem;
  }
  
  .carousel-card-title {
    font-size: 1rem;
  }
  
  .carousel-card-price {
    font-size: 1.35rem;
  }
}

/* ===================================
   SHARED SECTION STYLES
   =================================== */

.section-header {
  text-align: center;
  margin-bottom: 3rem;
}

.section-title {
  font-size: 2.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1rem;
}

.section-subtitle {
  color: #D1D5DB;
  font-size: 1.125rem;
  max-width: 600px;
  margin: 0 auto;
}

/* ===================================
   HOW IT WORKS SECTION
   =================================== */

.how-it-works-section {
  padding: 4rem 0;
  background: linear-gradient(135deg, #1a1a1a 0%, #252525 100%);
  text-align: center;
}

.how-it-works-section .section-title {
  margin-bottom: 0.5rem;
}

.how-it-works-section .section-subtitle {
  margin-bottom: 3rem;
}

.steps-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
}

.step-card {
  background: #1C1C1C;
  border: 2px solid #B8860B;
  border-radius: 16px;
  padding: 2.5rem 2rem;
  transition: all 0.4s ease;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-shadow: 0 0 12px rgba(184, 134, 11, 0.25), 0 0 24px rgba(184, 134, 11, 0.1);
  animation: pulse-glow 3s ease-in-out infinite;
}

.step-content {
  display: contents;
}

.step-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #B8860B 0%, #FFD700 100%);
  transform: scaleX(0);
  transition: transform 0.4s ease;
}

.step-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 0 20px rgba(184, 134, 11, 0.5), 0 12px 32px rgba(184, 134, 11, 0.25);
}

.step-card:hover::before {
  transform: scaleX(1);
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 12px rgba(184, 134, 11, 0.25), 0 0 24px rgba(184, 134, 11, 0.1);
  }
  50% {
    box-shadow: 0 0 16px rgba(184, 134, 11, 0.35), 0 0 32px rgba(184, 134, 11, 0.15);
  }
}

.step-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto 1.5rem;
  background: linear-gradient(135deg, rgba(184, 134, 11, 0.1) 0%, rgba(255, 215, 0, 0.1) 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.4s ease;
  padding: 0.5rem;
}

.step-card:hover .step-icon {
  background: linear-gradient(135deg, rgba(184, 134, 11, 0.2) 0%, rgba(255, 215, 0, 0.2) 100%);
  transform: scale(1.1) rotate(5deg);
}

.step-icon .custom-3d-icon {
  filter: drop-shadow(0 0 8px rgba(184, 134, 11, 0.4));
}

.step-card:hover .step-icon .custom-3d-icon {
  transform: scale(1.15) rotate(-5deg);
  filter: drop-shadow(0 0 16px rgba(255, 215, 0, 0.6));
}

.step-number {
  font-size: 0.875rem;
  font-weight: 700;
  color: #B8860B;
  letter-spacing: 2px;
  margin-bottom: 0.75rem;
}

.step-title {
  font-size: 1.5rem;
  font-weight: 700;
  color: white;
  margin-bottom: 1rem;
}

.step-description {
  color: #D1D5DB;
  line-height: 1.6;
  margin-bottom: 1.5rem;
  font-size: 0.95rem;
}

.step-features {
  list-style: none;
  padding: 0;
  margin: 0;
  text-align: left;
  background: #252525;
  padding: 1rem 1.5rem;
  border-radius: 8px;
  border: 1px solid #333333;
}

.step-features li {
  color: #D1D5DB;
  font-size: 0.9rem;
  padding: 0.4rem 0;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.how-it-works-cta {
  text-align: center;
  margin-top: 3rem;
}

.cta-button-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1rem 2.5rem;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  color: #1C1C1C;
  font-size: 1.125rem;
  font-weight: 700;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s ease;
  box-shadow: 0 4px 16px rgba(184, 134, 11, 0.3);
}

.cta-button-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(184, 134, 11, 0.5);
  background: linear-gradient(135deg, #FFD700 0%, #B8860B 100%);
}

.cta-button-primary svg {
  transition: transform 0.3s ease;
}

.cta-button-primary:hover svg {
  transform: translateX(4px);
}

/* ===================================
   SOCIAL PROOF SECTION
   =================================== */

.social-proof-section {
  padding: 4rem 0;
  background: #1C1C1C;
}

/* Stats Bar */
.stats-container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
  margin-bottom: 3rem;
  background: #252525;
  padding: 2rem;
  border-radius: 8px;
  border: 1px solid #333333;
}

.stats-container.stats-single {
  grid-template-columns: 1fr;
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
  margin-bottom: 3rem;
  text-align: center;
}

.stat-item {
  text-align: center;
}

.stat-value {
  font-size: 2.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-label {
  color: #D1D5DB;
  font-size: 0.875rem;
  margin-top: 0.5rem;
}

/* Featured Project Video */
.featured-project-container {
  margin-bottom: 3rem;
}

.featured-project-video {
  background: #252525;
  border-radius: 12px;
  overflow: hidden;
  border: 1px solid #333333;
  transition: all 0.3s ease;
}

.featured-project-video:hover {
  border-color: #B8860B;
  box-shadow: 0 8px 24px rgba(184, 134, 11, 0.2);
}

.video-wrapper {
  position: relative;
  width: 100%;
  padding-bottom: 56.25%; /* 16:9 Aspect Ratio */
  background: #1C1C1C;
  overflow: hidden;
}

.video-wrapper video,
.video-wrapper iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border: none;
  object-fit: cover;
}

.featured-video {
  background: #000;
}

.featured-project-info {
  padding: 2rem;
  background: #252525;
}

.featured-project-info h3 {
  font-size: 1.75rem;
  font-weight: 700;
  color: white;
  margin-bottom: 0.5rem;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.project-location {
  color: #B8860B;
  font-size: 1rem;
  font-weight: 600;
  margin-bottom: 1rem;
}

.project-description {
  color: #D1D5DB;
  font-size: 1rem;
  line-height: 1.6;
  margin: 0;
}

/* Testimonials */
.testimonials-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.testimonial-card {
  background: #252525;
  padding: 1.5rem;
  border-radius: 8px;
  border: 1px solid #333333;
  transition: all 0.3s ease;
}

.testimonial-card:hover {
  border-color: #B8860B;
  transform: translateY(-2px);
}

.testimonial-stars {
  color: #DAA520;
  font-size: 1.25rem;
  margin-bottom: 1rem;
}

.testimonial-quote {
  color: #D1D5DB;
  font-style: italic;
  line-height: 1.6;
  margin-bottom: 1rem;
}

.testimonial-author {
  color: #B8860B;
  font-weight: 600;
  margin-bottom: 0.25rem;
}

.testimonial-plan {
  color: #999;
  font-size: 0.875rem;
}

/* ===================================
   BRAND STORY SECTION
   =================================== */

.brand-story-section {
  padding: 4rem 0;
  background: #252525;
}

.brand-story-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
}

.brand-story-image {
  position: relative;
  background: #1a1a1a;
  border-radius: 8px;
  border: 1px solid #333333;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem;
}

.brand-story-image img {
  width: 100%;
  height: 100%;
  max-height: 500px;
  object-fit: contain;
  border-radius: 4px;
}

.image-badge {
  position: absolute;
  top: 1rem;
  left: 1rem;
  background: #B8860B;
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 50px;
  font-size: 0.875rem;
  font-weight: 600;
}

.content-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(184, 134, 11, 0.1);
  border: 1px solid #B8860B;
  padding: 0.5rem 1rem;
  border-radius: 50px;
  color: #DAA520;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 1.5rem;
}

.brand-story-title {
  font-size: 2rem;
  font-weight: 700;
  color: white;
  margin-bottom: 1.5rem;
}

.brand-story-text {
  color: #D1D5DB;
  line-height: 1.7;
  margin-bottom: 2rem;
}

.brand-story-text p {
  margin-bottom: 1rem;
}

.brand-features {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 2rem;
}

@media (max-width: 968px) {
  .brand-features {
    grid-template-columns: 1fr;
  }
}

.feature-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  color: #D1D5DB;
}

.feature-icon {
  color: #DAA520;
  font-weight: 700;
}

.brand-story-ctas {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.btn {
  padding: 0.875rem 1.75rem;
  border-radius: 8px;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s ease;
  display: inline-block;
}

.btn-primary {
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  color: white;
}

.btn-primary:hover {
  background: linear-gradient(135deg, #966F09 0%, #B8860B 100%);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(184, 134, 11, 0.4);
}

.btn-secondary {
  border: 2px solid #B8860B;
  color: #B8860B;
  background: transparent;
}

.btn-secondary:hover {
  border-color: #DAA520;
  color: #DAA520;
  background: rgba(184, 134, 11, 0.1);
}

/* ===================================
   CUSTOM STUDIO CTA SECTION
   =================================== */

.custom-studio-section {
  padding: 5rem 0;
  background: linear-gradient(135deg, rgba(37, 37, 37, 0.9) 0%, rgba(28, 28, 28, 0.9) 100%),
              url('https://hbfjdfxephlczkfgpceg.supabase.co/storage/v1/object/public/website/demos/Screenshot%202025-10-21%20212237.png') center/cover no-repeat;
  background-attachment: fixed;
  position: relative;
}

.studio-cta-container {
  max-width: 900px;
  margin: 0 auto;
}

.studio-cta-content {
  text-align: center;
}

.studio-badge {
  display: inline-block;
  padding: 0.5rem 1.25rem;
  background: rgba(184, 134, 11, 0.1);
  border: 1px solid #B8860B;
  border-radius: 50px;
  color: #DAA520;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 1.5rem;
}

.studio-title {
  font-size: 2.75rem;
  font-weight: 700;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1.5rem;
  line-height: 1.2;
}

.studio-subtitle {
  font-size: 1.25rem;
  color: #D1D5DB;
  line-height: 1.6;
  margin-bottom: 3rem;
  max-width: 750px;
  margin-left: auto;
  margin-right: auto;
}

.studio-features {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
  text-align: left;
}

.studio-feature {
  display: flex;
  gap: 1rem;
  align-items: flex-start;
  padding: 1.5rem;
  background: #1C1C1C;
  border: 1px solid #333333;
  border-radius: 12px;
  transition: all 0.3s ease;
}

.studio-feature:hover {
  border-color: #B8860B;
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(184, 134, 11, 0.2);
}

.studio-feature .custom-3d-icon {
  flex-shrink: 0;
  margin-top: 0.25rem;
  filter: drop-shadow(0 0 8px rgba(184, 134, 11, 0.3));
}

.studio-feature:hover .custom-3d-icon {
  transform: scale(1.15);
  filter: drop-shadow(0 0 12px rgba(184, 134, 11, 0.5));
}

.studio-feature h4 {
  color: white;
  font-size: 1.125rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.studio-feature p {
  color: #D1D5DB;
  font-size: 0.875rem;
  line-height: 1.5;
  margin: 0;
}

.studio-cta-buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-studio-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1.125rem 2.5rem;
  background: linear-gradient(135deg, #B8860B 0%, #FFD700 100%);
  color: #1C1C1C;
  font-size: 1.125rem;
  font-weight: 700;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s ease;
  box-shadow: 0 4px 16px rgba(184, 134, 11, 0.4);
}

.btn-studio-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(184, 134, 11, 0.6);
  background: linear-gradient(135deg, #FFD700 0%, #B8860B 100%);
}

.btn-studio-primary svg {
  transition: transform 0.3s ease;
}

.btn-studio-primary:hover svg {
  transform: translateX(4px);
}

.btn-studio-secondary {
  padding: 1.125rem 2.5rem;
  background: transparent;
  border: 2px solid #B8860B;
  color: #B8860B;
  font-size: 1.125rem;
  font-weight: 700;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.btn-studio-secondary:hover {
  background: rgba(184, 134, 11, 0.1);
  border-color: #DAA520;
  color: #DAA520;
  transform: translateY(-2px);
}

/* ===================================
   RESPONSIVE DESIGN
   =================================== */

@media (max-width: 1024px) {
  .brand-story-grid {
    grid-template-columns: 1fr;
  }
  
  .studio-features {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 768px) {
  .page-width {
    padding: 0 1rem;
  }
  
  .section-title {
    font-size: 2rem;
  }
  
  .how-it-works-section {
    padding: 2.5rem 0;
  }
  
  .how-it-works-section .section-subtitle {
    margin-bottom: 2rem;
    font-size: 1rem;
  }
  
  .steps-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
    margin-bottom: 2rem;
  }
  
  .step-card {
    padding: 1.5rem;
    flex-direction: column;
    gap: 0;
    text-align: left;
    border-radius: 12px;
    border-color: #B8860B;
    box-shadow: 0 0 10px rgba(184, 134, 11, 0.2), 0 0 20px rgba(184, 134, 11, 0.08);
    animation: pulse-glow-mobile 3s ease-in-out infinite;
  }
  
  @keyframes pulse-glow-mobile {
    0%, 100% {
      box-shadow: 0 0 10px rgba(184, 134, 11, 0.2), 0 0 20px rgba(184, 134, 11, 0.08);
    }
    50% {
      box-shadow: 0 0 14px rgba(184, 134, 11, 0.3), 0 0 28px rgba(184, 134, 11, 0.12);
    }
  }
  
  .step-card:hover {
    transform: translateY(-2px);
  }
  
  .step-icon {
    display: none;
  }
  
  .step-content {
    display: block;
    flex: 1;
  }
  
  .step-number {
    font-size: 0.75rem;
    margin-bottom: 0.25rem;
  }
  
  .step-title {
    font-size: 1.125rem;
    margin-bottom: 0.5rem;
  }
  
  .step-description {
    font-size: 0.875rem;
    margin-bottom: 0.75rem;
    line-height: 1.5;
  }
  
  .step-features {
    padding: 0.75rem 1rem;
  }
  
  .step-features li {
    font-size: 0.8rem;
    padding: 0.25rem 0;
  }
  
  .cta-button-primary {
    padding: 0.875rem 1.75rem;
    font-size: 1rem;
    width: 100%;
    justify-content: center;
  }
  
  .stats-container {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .featured-project-container {
    margin-bottom: 2rem;
  }
  
  .video-wrapper {
    padding-bottom: 75%; /* Taller aspect ratio on mobile */
  }
  
  .featured-project-info {
    padding: 1.5rem;
  }
  
  .featured-project-info h3 {
    font-size: 1.5rem;
  }
  
  .testimonials-grid {
    grid-template-columns: 1fr;
  }
  
  .brand-story-ctas {
    flex-direction: column;
  }
  
  .btn {
    width: 100%;
    text-align: center;
  }
  
  .custom-studio-section {
    padding: 3rem 0;
    background: linear-gradient(135deg, rgba(37, 37, 37, 0.9) 0%, rgba(28, 28, 28, 0.9) 100%),
                url('https://hbfjdfxephlczkfgpceg.supabase.co/storage/v1/object/public/website/demos/Screenshot%202025-10-21%20212237.png') left center/cover no-repeat !important;
    background-attachment: scroll !important;
  }
  
  .studio-title {
    font-size: 2rem;
  }
  
  .studio-subtitle {
    font-size: 1rem;
  }
  
  .studio-features {
    gap: 1rem;
  }
  
  .studio-feature {
    padding: 1.25rem;
  }
  
  .studio-cta-buttons {
    flex-direction: column;
  }
  
  .btn-studio-primary,
  .btn-studio-secondary {
    width: 100%;
    justify-content: center;
  }
}

@media (max-width: 480px) {
  .section-title {
    font-size: 1.75rem;
  }
  
  .stat-value {
    font-size: 2rem;
  }
}
</style>

<!-- Load authoritative floor plans data -->
<script src="{{ 'floor-plans-data.js' | asset_url }}"></script>

<script>
// Load products into carousel
document.addEventListener('DOMContentLoaded', function() {
  const carouselTrack = document.getElementById('carousel-track');
  
  // Fetch products from Shopify collection
  fetch('/collections/floor-plans/products.json?limit=20')
    .then(response => response.json())
    .then(data => {
      if (data.products && data.products.length > 0) {
        carouselTrack.innerHTML = '';
        
        // Define the priority order for first 4 products
        const priorityOrder = ['pedernales', 'serenity', 'whiskey', 'york-creek'];
        
        // Separate priority products and remaining products
        const priorityProducts = [];
        const otherProducts = [];
        
        data.products.forEach(product => {
          const handle = product.handle.toLowerCase();
          const priorityIndex = priorityOrder.findIndex(name => handle.includes(name));
          
          if (priorityIndex !== -1) {
            priorityProducts[priorityIndex] = product;
          } else {
            otherProducts.push(product);
          }
        });
        
        // Combine: priority products first (in order), then others
        const orderedProducts = priorityProducts.filter(p => p).concat(otherProducts).slice(0, 8);
        
        orderedProducts.forEach(product => {
          const card = document.createElement('a');
          card.href = `/products/${product.handle}`;
          card.className = 'carousel-card';
          
          // Get accurate specs from authoritative data
          const specs = window.FloorPlansData ? window.FloorPlansData.getSpecs(product.handle) : null;
          let specsHTML = '';
          
          if (specs) {
            const beds = specs.beds || '—';
            const baths = specs.baths || '—';
            const sqft = specs.area ? window.FloorPlansData.formatArea(specs.area) : '—';
            
            specsHTML = `
              <div class="carousel-card-specs">
                <span><strong>${beds}</strong> Beds</span>
                <span><strong>${baths}</strong> Baths</span>
                <span><strong>${sqft}</strong> SF</span>
              </div>
            `;
          }
          
          // Format price the same way as catalog page
          const variant = product.variants[0];
          const price = variant && variant.price 
            ? parseFloat(variant.price).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
            : '0.00';
          
          card.innerHTML = `
            <img src="${product.images[0]?.src || ''}" 
                 alt="${product.title}" 
                 class="carousel-card-image">
            <div class="carousel-card-content">
              <h3 class="carousel-card-title">${product.title}</h3>
              ${specsHTML}
              <div class="carousel-card-price">$${price}</div>
            </div>
          `;
          
          carouselTrack.appendChild(card);
        });
      }
    })
    .catch(error => {
      console.error('Error loading products:', error);
      carouselTrack.innerHTML = '<p style="color: #D1D5DB; text-align: center; width: 100%;">Unable to load products</p>';
    });
});

// Extract specs from product title or tags (not currently used in carousel)
function extractSpecs(product) {
  const specs = [];
  const title = product.title.toLowerCase();
  
  // Try to extract dimensions from title
  const sizeMatch = title.match(/(\d+)\s*x\s*(\d+)/);
  if (sizeMatch) {
    specs.push(`<span>📐 ${sizeMatch[1]}' x ${sizeMatch[2]}'</span>`);
  }
  
  // Try to extract bedrooms
  const bedMatch = title.match(/(\d+)\s*bed/i);
  if (bedMatch) {
    specs.push(`<span>🛏️ ${bedMatch[1]} Bed</span>`);
  }
  
  // Try to extract bathrooms
  const bathMatch = title.match(/(\d+(?:\.\d+)?)\s*bath/i);
  if (bathMatch) {
    specs.push(`<span>🚿 ${bathMatch[1]} Bath</span>`);
  }
  
  return specs.join('');
}
</script>

{% schema %}
{
  "name": "Store Homepage",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Hero Title",
      "default": "Premium Steel Building Plans"
    }
  ],
  "presets": [
    {
      "name": "Store Homepage"
    }
  ]
}
{% endschema %}

